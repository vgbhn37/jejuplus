<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.green.jejuplus.repository.interfaces.ReviewRepository">
	<select id="findReviewList" resultType="com.green.jejuplus.dto.contents.ReviewDto">
		select * from tb_review where contents_id = #{contentsId}
	</select>
	
	<insert id="insertReview">
		insert into tb_review(review_star, contents_id, user_id, review_content,created_at,review_recommend)
		values(#{reviewStar},#{contentsId},#{userId},#{reviewContent},now(),0)
	</insert>
	
	<update id="updateReview">
		UPDATE tb_review SET review_content = #{reviewContent}
		WHERE user_id = #{userId};
	</update>
	
	<delete id="deleteReview">
		DELETE FROM tb_review WHERE review_id = #{reviewId}
	</delete>
	
</mapper>